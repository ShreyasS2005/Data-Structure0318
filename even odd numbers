#include<stdio.h>
int main()
{
    int a[]={1,2,3,4,5};
    int n=sizeof(a)/sizeof(a[0]);
    int i;
    for(i=0;i<n;i++)
    {
        if(a[i]%2==0)
        printf("%d is even \n",a[i]);
        else
        printf("%d is odd \n",a[i]);
    }
    }
