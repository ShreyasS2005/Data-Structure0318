#include<stdio.h>
int main()
{
    int a[]={1,2,3,4,5};
    int n=sizeof(a)/sizeof(a[0]);
    int data=4;
    int i;
    for(i=0;i<n;i++)
    {
        if(a[i]==data)
        printf("array index:%d",i);
    }
}
